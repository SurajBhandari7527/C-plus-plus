#include<iostream>
using namespace std;
// this is the comment
int main(){ int m,n;
	cout<<"Enter any two numbers: ";
	cin>>m;
	cin >> n;
	//arithmetic operations
	int sum=m+n;
	int diff = m-n;
	int multi=m*n;
	float div=m/n;
	cout<<"sum is "<<sum<<"\n diff is "<<diff<<"\n multiplication is "<<multi<<"\n division is "<<div<<endl;
	
    return 0;
}
